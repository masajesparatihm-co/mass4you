// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // y 'tu-repositorio' por el nombre que le darás a este proyecto
  site: 'https://masajesparatihm-co.github.io/tu-repositorio',
});
